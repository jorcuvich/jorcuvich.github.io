/*
    Vite Dev-server configuration file for local development.
    @author jhotiori
*/

import { defineConfig } from "vite";

export default defineConfig({
    server: {
        host: "localhost",
        port: 8080
    }
})
